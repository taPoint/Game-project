#include "include/Army.h"
#include "include/Battle.h"
#include "include/Group.h"
#include "include/Commander.h"
#include "include/Warriors.h"

void Program() {
    // Initialize the game components here
    // For example, create armies, groups, and warriors

    // Example of creating a battle
    Battle battle;
    Group attack_group; // Assume this is initialized properly
    Group defending_group; // Assume this is initialized properly

    // Start the fight
    // battle.Fight(attack_group, defending_group);
}

int main() {
    Program();
    return 0;
}